#include <stdio.h>
#include<stdlib.h>

// typedef char DATA;
//     struct list
//     {
//         DATA d;
//         struct list *next;
//     };
//     typedef struct list ELEMENT;
//     typedef ELEMENT *LINK;

//     LINK STOL(char s[])
//     {
//         LINK head = NULL, tail;
//         int i;

//         if (s[0] != '\0')
//         {

//             head = (LINK)malloc(sizeof(ELEMENT));

//             head->d = s[0];

//             tail = head;

//             s[0];

//             for (i = 1; s[i] != '\0'; i++)
//             {

//                 tail->next = (LINK)malloc(sizeof(ELEMENT));
//                 tail = tail->next;
//                 tail->d = s[i];
//             }
//             tail->next = NULL;
//         }
//         return head;
//     }

int main()
{
    // struct abc{
    //     int a;
    //     char b;
    //     double c;
    //     struct abc* next;
    // }dat1;
    // dat1.a = 57;
    // dat1.b = 'm';
    // dat1.c = 32.5;
    // dat1.next->a = 34;
    // FILE *ptr;
    // ptr = fopen("abc.txt","r");
    // // char line[1000];
    // // while (fgets(line,10,ptr))
    // // {
    // //     printf("%s",line);
    // //     break;
    // // }
    // char c;
    // printf("Pos: %ld",ftell(ptr));
    // c = fgetc(ptr);
    // printf("\nPos: %ld",ftell(ptr));
    // fseek(ptr, 4, SEEK_CUR);
    // printf("%c",fgetc(ptr));
    // printf("\nPos: %ld",ftell(ptr));
    
    // fprintf(stdout,"Hello World\n");
    // printf("\nPos: %ld",ftell(ptr));
    
    
}